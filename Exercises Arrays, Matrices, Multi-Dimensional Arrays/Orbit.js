/**
 * Created by Stoyan on 4.6.2017 г..
 */
function  createMatrix (arr) {
    let rows = arr[0];
    let cols = arr[1];
    let rowPos = arr[2];
    let colPos = arr[3];

    let matrix = [];

    for (let i = 0; i < rows; i++) {
        matrix[i] = [];
    }
    matrix[rowPos][colPos] = 1;

    for (let i = rowPos.pos)
}
